# CodexMate 0.0.44

- Fixes pinned thread menus so hidden project threads are not backfilled when pinned items consume the visible project roots.
- Prevents large project sections from suddenly expanding after several threads are pinned.
- Adds regression coverage for pinned thread sections and project menu visibility.
