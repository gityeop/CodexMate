# CodexMate 0.0.31

- Hides unhydrated running thread placeholders from the notch menu until their project and title metadata is available.
- Keeps running-state discovery active while avoiding temporary `Unknown Project` entries that only show a thread ID.
