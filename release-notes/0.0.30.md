# CodexMate 0.0.30

- Keeps tracked remote threads stable even when they fall outside the recent thread list window.
- Prevents stale local desktop turn-start hints from reviving a remote thread after newer remote metadata already marked it idle.
