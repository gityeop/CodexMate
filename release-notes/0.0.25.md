# CodexMate 0.0.25

- Keeps the notch status responsive when a user-input wait clears while the notch is closed.
- Limits the faster waiting-state polling to lightweight desktop activity checks, keeping full thread-list refreshes on the idle cadence until the notch menu is opened.
