name: CI

on:
  push:
  pull_request:

jobs:
  swift-test:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Resolve package dependencies
        run: swift package resolve
      - name: Run tests
        run: swift test
